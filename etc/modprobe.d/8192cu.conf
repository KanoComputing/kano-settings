# disable power safe and suspend modes in the wireless device
# should eliminate random network outages when the PI becomes idle
#
# look at /sys/modules/8192cu/parameters to see currently used settings
#

options 8192cu rtw_power_mgnt=0 rtw_enusbss=0
