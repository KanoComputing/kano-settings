#!/bin/bash

# postrm
#
# Copyright (C) 2014 Kano Computing Ltd.
# License: http://www.gnu.org/licenses/gpl-2.0.txt GNU General Public License v2
#

case "$1" in

    remove)

        # Restore original rc.lcaol and remove Kano version
        mv -fv $rclocal_backup /etc/rc.local
        rm -fv /etc/kano-rc.local

    upgrade)
        # Remove config file
        rm /etc/sudoers.d/kano-settings_conf

        # TODO: Disable the proxy if enabled

        # Remove dante libc simbolic link
        rm -fv /lib/arm-linux-gnueabihf/libc.so

        # Upgrade rc.local provided by Kano
        cp -fv /etc/kano-rc.local /etc/rc.local
        ;;
esac

#DEBHELPER#

exit 0
